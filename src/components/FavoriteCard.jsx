import React from 'react';

export default function FavoriteCard({filter, favoriteRecipes}) {
  console.log(filter);
  console.log(favoriteRecipes);
  return (
    <section>
      FavoriteCard
    </section>
  );
}
